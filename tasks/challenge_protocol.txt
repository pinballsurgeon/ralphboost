Design a strict evaluation protocol for RalphBoost on real-world tasks.
Requirements:
- Output must follow the JSON schema in the system instructions.
- Exactly 3 hypotheses with numeric targets and explicit thresholds.
- Exactly 3 metrics, each formula_or_calc must reference telemetry fields.
- One metric must explicitly measure false completion or evidence mismatch.
- micro_tasks list must have at least 12 tasks and include:
  - A/B ablation
  - sanity-check
  - regression guard
  - latency profiling
  - token budget analysis
- anti_gaming must include at least 3 hidden checks and explain withholding.
- actions must include:
  - write_file to a .md protocol
  - note describing log fields for latency_ms, total_tokens, token_budget
Return ONLY valid JSON.
